# tichu-trainer

보드게임 **티츄**의 규칙을 읽기만 하지 않고 직접 풀어보며 익히는 연습 사이트.
빌드 도구 없는 순수 ES 모듈 + 해시 라우팅 SPA. `index.html` 을 열면 그게 곧 배포본이다.

## 실행과 검증

```bash
node tools/serve.js 8000     # 의존성 없는 정적 서버 (no-store)
node tests/run.js            # 79개 테스트. 엔진 로직 + 콘텐츠 정합성
```

- 카드 56장을 한눈에 보려면 `http://localhost:8000/tests/cards.html`
- 화면을 손봤으면 **라이트/다크 두 테마, 375px·1280px 두 폭**을 모두 확인한다.

## 구조

```
index.html          껍데기만. 헤더·푸터·<main id="app"> 과 CSS link
styles/
  tokens.css        모든 색·간격·크기. 라이트=:root, 다크=[data-theme='dark'] 두 벌
  base.css          리셋과 레이아웃 유틸
  components/       화면을 가리지 않고 쓰이는 부품 — card, control, surface, table
  pages/            화면 전용 — home, lesson, quiz, minigame, sandbox
src/
  engine/           게임 규칙. DOM 을 모른다. 테스트는 여기가 중심
  ui/               재사용 부품 — card-view, hand-view, dom, markup, verdict
  views/            화면. lesson/ 과 minigame/ 아래에 그 화면 전용 모듈
                    lesson/spotlight.js 가 본문을 따라다니는 펠트 판이다
  data/             챕터·레슨·미니판 콘텐츠
```

**마크업은 전부 JS 가 만든다.** `index.html` 에는 구조가 없다. 화면 구조를 바꾸려면
`src/views/` 의 해당 파일을 열어야 한다.

## 규칙

### 크기는 6단 스케일에서만

한 화면에 폰트 크기가 15종 섞여 위계가 안 읽히던 걸 여섯 개로 묶었다. **이 여섯 개 밖의
크기를 새로 쓰지 않는다.** `--fs-display / title / section / body / small / micro` 이고
각각에 `--lh-*` 줄간격이 짝지어져 있다. 560px 아래에서 display·title 만 한 단계 내려간다.

서체는 **Gothic A1 한 벌**(Google Fonts, `index.html` 의 `<link>`)이라 위계의 절반을
무게가 맡는다 — `--weight-display(900) / strong(700) / medium(500) / body(400)`.
웹폰트가 안 뜨면 `--font` 의 폴백(system-ui)으로 떨어지고 화면은 그대로 선다.

문단은 화면 폭이 아니라 `--measure`(52ch)로 묶는다. 940px 를 그대로 쓰면 한글 한 줄이
55자가 된다. `.measure` 유틸이 있다.

**예외는 카드 면(SVG)뿐이다.** 카드는 자체 잉크 토큰을 쓰는 독립된 인쇄물이라 페이지
타이포와 단계를 공유하지 않는다. 그래서 화면별 크기 종류를 세면 레슨이 11종으로 나오는데
그중 5종이 카드 면이다.

### 색은 tokens.css 에서만

JS·CSS 어디에도 색 리터럴을 두지 않는다. 현재 하드코딩된 색은 0건이고 그대로 유지한다.

**카드 면은 라이트·다크 모두 밝다**(실물 카드가 그렇고 특수카드 삽화가 크림색 바탕이라).
그래서 색이 두 갈래다. 헷갈리면 다크 모드에서 대비가 깨진다.

| 토큰 | 어디에 |
|---|---|
| `--suit-*` | 페이지 배경 위 — 본문 속 카드 칩. 테마를 따라간다 |
| `--suit-ink-*` | **카드 면 위** — 테마와 무관하게 진한 쪽으로 고정 |
| `--card-ink` | 카드에 인쇄되는 글자 |
| `--felt-*` | 펠트 위의 반투명 층. 펠트는 늘 어두워서 흰/검정 알파를 쓴다 |
| `--header-*` | 상단 바. 두 테마 모두 어둡다 |

**짝을 섞지 않는다.** `--bg-sunken`(페이지) 배경에 `--card-ink`(카드 면) 글자를 얹었더니
다크에서 대비가 1.15까지 떨어져 글자가 안 보였다. 배경과 글자는 같은 갈래에서 고른다.

`opacity` 로 글자를 흐리게 깔면 대비 계산이 어긋난다. 펠트 위 12~14px 짜리는 흐림만으로도
4.5:1 아래로 떨어지니, 크기와 자간으로 낮추고 색은 불투명하게 둔다.

### 표면은 자기 여백을 갖는다

`.felt` 는 배경·모서리·그림자에 더해 **안쪽 여백까지** `components/table.css` 가 정한다.
이 규칙이 한때 `pages/minigame.css` 에 전역 선택자로 있어서, 홈과 레슨의 펠트가 미니판
여백을 따라갔다. 화면별로 다르게 쓰려면 `pages/` 에서 덮는다.

### 카드 삽화

특수카드 4장(용·봉황·개·마작)만 `assets/cards/*.webp` 삽화를 쓴다. `SPECIAL` 값이 곧 파일 이름이다.
원본 PNG 는 `assets/cards/original/` 에 있다 — 다시 생성하면 같은 그림이 안 나오므로 잘라내기를
고치려면 원본이 필요하다. 숫자카드와 수트 문양은 SVG 로 그린다.

**삽화를 추가·교체할 일이 생기면 프롬프트를 써서 사용자에게 넘긴다.** 이 환경에는 이미지 생성
도구가 없다. SVG 로 수채 일러스트를 흉내 내려 하지 않는다.

### 커밋

- `feat(scope):` `fix(scope):` `refactor(scope):` `docs:` — 본문은 **왜** 를 적는다
- 기능별로 유사한 변경끼리 묶는다. 한 덩어리로 몰지 않는다
- push 전 `git fetch` 후 `origin/main` 과 비교한다

## 도구

`.mcp.json` 에 **Chrome DevTools MCP** 가 들어 있다(clone 하면 따라온다). 스크린샷·Lighthouse·
접근성·색 대비 감사용. 2026-08 세션에서 스크린샷·Lighthouse 모두 정상 동작을 확인했다.

- 폭을 바꿀 때는 `resize_page` 가 아니라 **`emulate` 의 viewport** 를 쓴다. 창이 최대화
  상태면 `resize_page` 는 `Restore window to normal state` 로 실패한다.
- CSS·JS 를 고친 뒤에는 **`ignoreCache: true` 로 새로고침**한다. 서버가 no-store 라도
  크롬이 캐시를 들고 있어, 고친 게 반영 안 된 화면을 재고 있던 적이 있다.
- `take_screenshot` 이 가끔 타임아웃을 낸다. 한 번 더 부르면 찍힌다.

### 대비 측정 스크립트

`evaluate_script` 로 페이지 안에서 직접 재는 방식이 Lighthouse 보다 촘촘하다. 다만 세 가지를
챙겨야 값이 맞는다 — 이걸 빠뜨려 오탐·누락이 났었다.

1. `color(srgb ...)` 표기를 rgb 로 환산한다 (헤더 배경이 이 표기였다)
2. **부모의 `opacity` 를 누적**해 실효 전경색을 계산한다
3. 그라디언트 배경(`.felt`)은 `backgroundColor` 가 투명이라 `backgroundImage` 에서 색을 꺼낸다

카드 면 안의 SVG 글자는 이 방식으로 못 잰다(배경이 CSS 가 아니라 `<rect fill>`). `svg` 안쪽은
건너뛰고, 카드 대비는 눈으로 확인한다. `disabled` 컨트롤은 WCAG 대상 밖이니 제외한다.

---

# 디자인 정비 — 끝난 일 (2026-08)

시안 없이 구현부터 해서 화면이 정돈되지 않았던 걸 **감사 → 시안 → 구현** 순서로 되잡았다.
`ade9b71` 이후 `main` 에 다 올라가 있다.

## 방향 — B "판"

세 방향(룰북 / 판 / 사다리)을 아트보드에 올려 눈으로 고른 결과다.
**사이트가 곧 게임판**이라는 쪽. 홈은 손패 부채가 히어로고, 레슨은 읽는 형태가 옆 테이블에
계속 깔려 있고, 퀴즈·미니판은 판이 화면의 중심이다.

시안 캔버스: https://claude.ai/code/artifact/a51a532e-64d1-420d-8520-7b13b349f10a

## 무엇이 바뀌었나

| 커밋 | 무엇 |
|---|---|
| `a35bb4c` `54c6847` | 다크에서 안 보이던 글자, 대비 미달 텍스트, 제목 계층, 포커스 링. 접근성 94 → 100 |
| `5c9d2fd` | 타입 스케일 6단, Gothic A1, 상단 바 토큰, 탭 타깃 44px |
| `0eac90f` | 홈 — 펠트 히어로 + 손패 부채, 카드 모양 챕터 번호, 펠트 배너 |
| `89b64eb` | 레슨 — 본문 옆에 따라다니는 펠트 판. 카드가 본문에서 판으로 올라간다 |
| `2577fcb` | 퀴즈 — 문제 문장이 주인공. 미니판 — 설명을 옆 단으로 빼 판·손패·버튼을 한 화면에 |

## 다음에 화면을 손볼 때

**만들고 나서 다듬는 순서로 돌아가지 않는다.** 구조를 바꿀 일이면 `frontend-design` 으로
방향을 정하고 `design` 으로 시안을 올려 눈으로 고른 뒤에 코드를 건드린다.

값(무게·간격·크기)은 토큰에 몰려 있으니 구현하면서 조정해도 된다. 되돌리기 어려운 건
구조지 값이 아니다.

## 남은 것

- **레슨의 캡션 없는 절** — 카드가 판으로 올라가서 "3. 트리플" 같은 절은 제목만 남는다.
  캡션을 한 줄 넣거나, 캡션 없는 절은 카드를 본문에 남기는 쪽을 고를 수 있다.
- **판의 조합 이름** — `detectCombo` 로 자동 생성했다가 걷어냈다. 본문의 카드 묶음이 늘
  조합인 게 아니라서(덱 챕터의 점수 카드가 "포카드 폭탄 A", 마작 한 장이 "싱글 1") 틀린
  말을 했다. 필요하면 `cards` 블록에 이름 필드를 두고 콘텐츠가 직접 말하게 한다.
- **미니판 375px** — 헤더·제목·목표·판·손패·버튼이 한 화면(812px)에 다 안 들어온다.
  버튼 아래끝이 982px 라 판을 본 상태에서 170px 쯤 내려야 한다. 목표를 감추거나 판을 더
  줄이지 않고는 안 되는데 둘 다 손해가 커 보여 멈췄다.
- **CLS 0.127** — 첫 페인트 때 `#app` 이 비어 있고 JS 가 나중에 채우는 구조 탓. 아키텍처
  특성이라 우선순위 낮다.
- **카드 버튼의 `label-content-name-mismatch`** — 삽화 카드가 이름과 점수를 따로 그려
  보이는 글자가 "25용" 처럼 이어진다. 사람이 말할 이름("용")은 접근 이름에 있어서 실제
  실패로는 이어지지 않아 뒀다.

## 참고

- 남은 리팩토링 항목은 `리팩토링 계획.md` 에 있다. JS 구조 정리는 PR #1 로 끝났다
- 규칙 출처는 Fata Morgana / Rio Grande 공식 룰북
