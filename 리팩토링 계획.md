# 리팩토링 계획

디자인 작업 전에 코드를 점검하면서 정리한 목록. CSS·JS 정리는 끝났고, 남은 건 뷰 계층 테스트뿐이다.

---

## 끝난 것

### `fix(tools)` 개발 서버 캐시

`no-cache` 는 재검증만 요구하는데 `tools/serve.js` 가 ETag 를 안 보내서, 파일을 고쳐도
브라우저가 옛날 것을 계속 쓰는 일이 있었다. `no-store` 로 바꿈.

### `refactor(styles)` 테마 팔레트 중복 제거

팔레트가 `:root`, `@media dark`, `[data-theme=light]`, `[data-theme=dark]` 네 벌로 적혀 있어
색 하나 바꾸려면 세 곳을 고쳐야 했다. `index.html` 에서 첫 페인트 전에 `data-theme` 을 심게 하고
팔레트는 라이트·다크 두 벌만 남김 (197줄 → 152줄). 새로고침 때 흰 화면이 번쩍이던 것도 같이 해결.

- 컨테이너 테두리를 `--edge` / `--edge-strong` 두 토큰으로 모아 11곳이 참조
- 토큰 밖에 있던 색 6곳을 `--felt-sheen`, `--felt-highlight`, `--felt-hairline`, `--felt-well`, `--on-solid` 로
- 매직 넘버를 `--grid-min`, `--log-max-h`, `--radius-pill` 로
- 헤더 전용이던 `.site-header__spacer` → `.spacer` (샌드박스와 레슨 네비게이션이 헤더 클래스를 빌려 쓰고 있었음)
- 미사용 `.visually-hidden`, `.panel--flush`, `.note--ok` 제거

### `refactor(styles)` CSS 파일 분리

461줄짜리 `components.css` 하나에 카드·버튼·챕터목록·펠트·퀴즈·샌드박스가 다 있었다.
`src/` 구조와 같은 모양으로 나눔.

```
styles/
  tokens.css          토큰
  base.css            리셋, 앱 셸, 레이아웃 유틸, 브레이크포인트 메모
  components/         화면을 가리지 않고 쓰이는 것
    card.css          카드, 손패 줄, 빈 자리, 문장 속 카드 칩
    control.css       버튼, select
    surface.css       패널, 알림 상자, 배지, 판정 상자, 진도 막대
  pages/              그 화면에서만 쓰이는 것
    home.css  lesson.css  quiz.css  minigame.css  sandbox.css
```

### `refactor` 죽은 코드 제거

패키지를 나누기 전에 먼저 지웠다. 전체 export 를 훑어 src/ 와 tests/ 어디에서도
참조하지 않는 15개(TEAM_LABEL, findBombs, hasLegalPlay, groupByType, formatHand,
isSpecial, isMahjong, gameResult, turnInfo, beats, quizById, quizResult, subscribe,
setOnNavigate, 그리고 쓰지 않던 import 2개)와 거기 딸린 코드 — 구독자가 없던
listeners 집합, 아무도 설정하지 않던 onNavigate 호출 — 를 없앴다.

같은 맥락으로 `recordQuiz(chapterId, quizId, correct)` 의 correct 인자도 뺐다.
정답 경로에서만 호출되어 true 밖에 들어간 적이 없고, 'wrong' 을 읽던 quizResult 도
위에서 지웠다. 오답 이력이 필요해지면 그때 다시 넣는다.

### `refactor(ui)` 관심사가 섞인 모듈 쪼개기

`ui/format.js` 하나에 있던 셋을 갈랐다.

| 옮긴 것 | 간 곳 |
|---|---|
| `element()` | `ui/dom.js` |
| `verdictBox()` | `ui/verdict.js` |
| `describeCombo()`, `formatPoints()` | `engine/describe.js` |

`describe.js` 를 `engine/` 에 둔 이유: 도메인 개념을 한국어로 옮기는 일이고, 이미
`COMBO_LABEL`(combos.js), `SEAT_LABEL`(seats.js), `CALL_LABEL`(scoring.js) 이 같은 성격으로
engine 안에 있다. DOM 의존도 없다.

레슨 전용인 `blocks.js` 가 품고 있던 공용 함수 `inline()` 은 `ui/markup.js` 로 뺐다.
퀴즈와 미니판이 레슨 렌더러를 import 하던 연결이 이걸로 끊겼다. 두 벌이던
수트 → CSS 변수 매핑도 `card-view.js` 가 `suitColor(card)` 를 export 하고
`markup.js` 가 그걸 쓰는 것으로 하나가 됐다.

덤으로, 뷰 곳곳에 흩어져 있던

```js
const note = element('div', 'note');
note.innerHTML = inline(...);
```

패턴 10곳을 `markup.js` 의 `htmlElement()` / `noteElement()` 로 모았다.
`blocks.js` 안에만 있던 `el()` 도 같은 함수여서 `htmlElement` 로 대체됐다.

### `refactor(ui)` verdictBox 가 본문을 직접 받게

본문을 빈 문자열로 넘긴 뒤 `box.querySelector('.verdict__body').innerHTML = ...` 로
채우던 방식 때문에 객관식 퀴즈 전체가 무반응이던 버그가 있었다.
`verdictBox(tone, title, body, { html })` 로 바꿔 querySelector 를 없앴다.
기본은 textContent 라 마크업을 넣는 곳만 옵션을 켠다.

이번 리팩토링에서 **동작이 바뀌는 건 이 커밋뿐이라** 이동 커밋과 섞지 않았다.

### `refactor(views)` 화면 전용 모듈을 화면 폴더로

```
ui/table-view.js  ->  views/minigame/table-view.js
ui/blocks.js      ->  views/lesson/blocks.js
ui/demos.js       ->  views/lesson/demos/   (챕터별 3개 + 레지스트리)
```

`main.js` 안에 인라인으로 있던 `sandboxView` 는 `views/sandbox-view.js` 로,
레슨 뷰가 export 하고 미니판 뷰가 import 하던 `notFoundPanel` 은
`views/not-found-view.js` 로 뺐다.

### 지금 구조

```
src/
  engine/           도메인 규칙 (DOM 모름) + describe.js
  ui/               어느 화면에서나 쓰는 부품
    dom.js  markup.js  verdict.js  card-view.js  hand-view.js  sandbox.js
  views/
    home-view.js  quiz-view.js  sandbox-view.js  not-found-view.js
    lesson-view.js
    lesson/         blocks.js, demos/
    minigame-view.js
    minigame/       table-view.js
  data/  store/     (그대로 — 이미 잘 나뉘어 있었다)
```

---

## 남은 것 — 뷰 계층 테스트

테스트 79개가 전부 engine 과 콘텐츠 검증이고 뷰는 한 줄도 덮여 있지 않다.
그런데 지금까지 난 버그(퀴즈 무반응)도, 이번에 유일하게 동작을 바꾼 부분
(`verdictBox`)도 전부 뷰다. 빌드 도구 없는 이 프로젝트에 jsdom 을 들이는 건
과해 보이니, 최소 DOM 스텁으로 `verdictBox` · `inline` 같은 순수 생성 함수만
덮는 선을 고민 중이다.

## 손대지 않기로 한 것

점검하면서 확인했지만 문제가 아니어서 그대로 두는 것들. 다시 들여다보지 않아도 된다.

- **테두리를 남발한 구조가 아니다.** `border` 검색 67건 중 대부분은 `border-radius` 다.
  실제 선은 컴포넌트 테두리 16개와 구분선 3개(로그 행, 표 행, 레슨 하단)뿐이고 전부 토큰을 쓴다.
  영역 구분은 선이 아니라 배경 + 패딩 + 라운드로 하고 있다.
- **JS 에 하드코딩된 색은 0건.** 간격·라운드·그림자도 전부 토큰.
- **카드 크기는 `--card-w` 하나로 조절된다.** 라운드도 `calc(var(--card-w) * 0.09)` 로 따라온다.
  특정 영역의 카드만 키우려면 그 컨테이너에 `--card-w` 만 다시 주면 된다.
- **카드 SVG 좌표는 매직 넘버지만 `card-view.js` 의 `faceSvg()` 안에 갇혀 있다.**
  viewBox 가 100×140 로 고정이라 카드 얼굴을 새로 그려도 그 함수만 고치면 된다.
- **특이도 높은 선택자가 거의 없다.** `button.card`, `table.data`, `.felt .seat__name` 정도라
  덮어쓰기 싸움이 안 난다. `!important` 는 `prefers-reduced-motion` 한 곳뿐.
- **`engine/`, `data/`, `store/` 는 이미 잘 나뉘어 있다.** engine 이 DOM 을 모르는 것도 유지되고 있고,
  챕터·미니판도 파일별로 분리되어 있다.

---

## 디자인 작업할 때 알아둘 것

**마크업이 HTML 파일이 아니라 JS 에서 만들어진다.** 색·간격·테두리는 CSS 만 고쳐도 되지만,
구조를 바꾸는 것(예: 챕터 카드에 썸네일 추가)은 뷰 파일을 열어야 한다.

| 화면 | 마크업 위치 | 스타일 위치 |
|---|---|---|
| 챕터 목록 | `views/home-view.js` | `styles/pages/home.css` |
| 레슨 | `views/lesson-view.js` + `views/lesson/blocks.js` | `styles/pages/lesson.css` |
| 퀴즈 | `views/quiz-view.js` | `styles/pages/quiz.css` |
| 미니판 | `views/minigame-view.js` + `views/minigame/table-view.js` | `styles/pages/minigame.css` |
| 샌드박스 | `views/sandbox-view.js` + `ui/sandbox.js` | `styles/pages/sandbox.css` |
| 카드 | `ui/card-view.js` (SVG 직접 생성) | `styles/components/card.css` |

색을 바꾸려면 `styles/tokens.css` 만 보면 된다. 라이트는 `:root`, 다크는 `:root[data-theme='dark']`.

작업 중에는 `node tools/serve.js 8000` 으로 띄우고, 고친 뒤에는 `node tests/run.js` 로
79개 테스트가 그대로 통과하는지 확인하면 된다.
